[alias]
        tree = log --oneline --decorate --graph --branches --tags --remotes
        st = status
        co = checkout
        sw = switch
        swc = switch -c

[user]
        email = {{ .email }}
        name = {{ .name }}

